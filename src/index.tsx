import {BrowserRouter, history as BrowserHistory} from './BrowserRouter'
import {HashRouter, history as HashHistory} from './HashRouter'
import {ROUTE_CHANGE} from './utils'

export const ROUTE_CHANGE_ACTION = ROUTE_CHANGE
export {BrowserRouter, BrowserHistory, HashRouter, HashHistory}
